require 'spec_helper'

describe Pagerduty::CLT do
  it 'has a version number' do
    expect(Pagerduty::CLT::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
